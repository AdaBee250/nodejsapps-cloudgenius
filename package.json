{
  "name": "nodejsapps",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "rm -rf build && mkdir build && find . -maxdepth 1 ! -name 'build' ! -name '.' -exec cp -R {} build/ \\; && echo 'Build complete!'",
    "prepare": "npm run build"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.0"
  }
}
