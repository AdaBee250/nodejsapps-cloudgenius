{
  "name": "nodejsapps",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "if [ ! -d build ]; then mkdir build; fi && cp -R * build/ && echo 'Build complete!'",
    "prepare": "npm run build"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.0"
  }
}
