{
  "name": "nodejsapps",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "if [ -d build ]; then echo 'Build directory already exists, skipping creation.'; else mkdir -p build && cp -R * build/ && echo 'Build complete!'; fi",
    "prepare": "npm run build"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.0"
  }
}
